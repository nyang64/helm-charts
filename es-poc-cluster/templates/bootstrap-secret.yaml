{{- if and .Values.security.enabled (not .Values.elasticPassword.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "es-poc-cluster.fullname" . }}-bootstrap
  labels:
    {{- include "es-poc-cluster.labels" . | nindent 4 }}
type: Opaque
stringData:
  ELASTIC_PASSWORD: {{ .Values.elasticPassword.password | quote }}
{{- end }}
