apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "es-poc-cluster.fullname" . }}-config
  labels:
    {{- include "es-poc-cluster.labels" . | nindent 4 }}
data:
  elasticsearch.yml: |
    cluster.name: {{ .Values.clusterName | quote }}
    network.host: "0.0.0.0"

    {{- if .Values.security.enabled }}
    xpack.security.enabled: true
    {{- if .Values.tls.enabled }}
    # xpack.security.ssl.reload.enabled: ES watches cert files on disk and hot-reloads them
    # when cert-manager rotates the Secret, without requiring a pod restart.
    # Documented in ES 8.x: https://www.elastic.co/guide/en/elasticsearch/reference/8.17/security-settings.html
    # If this causes startup errors on your ES version, remove it and use the reload API instead:
    #   POST /_nodes/reload_secure_settings
    xpack.security.ssl.reload.enabled: true
    xpack.security.transport.ssl.enabled: true
    xpack.security.transport.ssl.certificate: /usr/share/elasticsearch/config/certs/tls.crt
    xpack.security.transport.ssl.key: /usr/share/elasticsearch/config/certs/tls.key
    xpack.security.transport.ssl.certificate_authorities: /usr/share/elasticsearch/config/certs/ca.crt
    xpack.security.transport.ssl.verification_mode: certificate
    {{- if not .Values.istio.enabled }}
    xpack.security.http.ssl.enabled: true
    xpack.security.http.ssl.certificate: /usr/share/elasticsearch/config/certs/tls.crt
    xpack.security.http.ssl.key: /usr/share/elasticsearch/config/certs/tls.key
    xpack.security.http.ssl.certificate_authorities: /usr/share/elasticsearch/config/certs/ca.crt
    {{- end }}
    {{- end }}
    {{- else }}
    xpack.security.enabled: false
    {{- end }}

    {{- if and .Values.jwt.enabled .Values.security.enabled }}
    xpack.security.authc.realms.jwt.jwt1:
      order: 2
      token_type: {{ .Values.jwt.tokenType | quote }}
      client_authentication.type: {{ .Values.jwt.clientAuthentication | quote }}
      allowed_issuer: {{ .Values.jwt.allowedIssuer | quote }}
      allowed_audiences: {{ .Values.jwt.allowedAudiences | toJson }}
      claims.principal: {{ .Values.jwt.principalClaim | quote }}
      {{- if .Values.jwt.groupsClaim }}
      claims.groups: {{ .Values.jwt.groupsClaim | quote }}
      {{- end }}
      pkc_jwkset_path: {{ .Values.jwt.jwksetPath | quote }}
      {{- if .Values.jwt.allowedSignatureAlgorithms }}
      allowed_signature_algorithms: {{ .Values.jwt.allowedSignatureAlgorithms | toJson }}
      {{- end }}
    {{- end }}
    {{- if and .Values.ad.enabled .Values.security.enabled }}
    xpack.security.authc.realms.active_directory.ad1:
      order: 3
      domain_name: {{ .Values.ad.domainName | quote }}
      url: {{ .Values.ad.url | quote }}
      bind_dn: {{ .Values.ad.bindDn | quote }}
      bind_password: "${AD_BIND_PASSWORD}"
      user_search:
        base_dn: {{ .Values.ad.userSearchBaseDn | quote }}
      group_search:
        base_dn: {{ .Values.ad.groupSearchBaseDn | quote }}
      {{- if .Values.ad.sslCertificateAuthoritiesSecret }}
      ssl.certificate_authorities: "/usr/share/elasticsearch/config/certs/ad-ca/ca.crt"
      {{- else if .Values.ad.sslCertificateAuthorities }}
      ssl.certificate_authorities: {{ .Values.ad.sslCertificateAuthorities | quote }}
      {{- end }}
    {{- end }}
