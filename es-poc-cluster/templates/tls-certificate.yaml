{{- if .Values.tls.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "es-poc-cluster.fullname" . }}
  labels:
    {{- include "es-poc-cluster.labels" . | nindent 4 }}
spec:
  secretName: {{ include "es-poc-cluster.tlsSecretName" . }}
  duration: {{ .Values.tls.duration }}
  renewBefore: {{ .Values.tls.renewBefore }}
  # usages must include both serverAuth and clientAuth because ES transport TLS is mutual:
  # each node simultaneously acts as TLS server (accepting connections) and TLS client
  # (initiating connections). JDK 17+ enforces Extended Key Usage -- omitting either EKU
  # causes the TLS handshake to fail and prevents cluster formation.
  usages:
    - server auth
    - client auth
    - digital signature
    - key encipherment
  issuerRef:
    name: {{ include "es-poc-cluster.tlsIssuerName" . }}
    kind: Issuer
  dnsNames:
    - {{ include "es-poc-cluster.fullname" . }}
    - {{ include "es-poc-cluster.fullname" . }}-master-headless
    - {{ include "es-poc-cluster.fullname" . }}-data-headless
    - {{ include "es-poc-cluster.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
    - {{ include "es-poc-cluster.fullname" . }}-master-headless.{{ .Release.Namespace }}.svc.cluster.local
    - {{ include "es-poc-cluster.fullname" . }}-data-headless.{{ .Release.Namespace }}.svc.cluster.local
    - "*.{{ include "es-poc-cluster.fullname" . }}-master-headless.{{ .Release.Namespace }}.svc.cluster.local"
    - "*.{{ include "es-poc-cluster.fullname" . }}-data-headless.{{ .Release.Namespace }}.svc.cluster.local"
{{- end }}
